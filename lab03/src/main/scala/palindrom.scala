/*object palindrom extends App{
  def jestPalindromem(tab:Array[Int]):Boolean={
    def helper(tab:Array[Int],acc:Int=0):Boolean={

        val tabsize = tab.size-(acc+1)
        if(doszlismy do konca czy tam srodka nie wiem) true
        else if(tab(acc)==tab(tabsize)) helper(tab,acc+1)
        else false

    }
    if (helper(tab)) true
    else false
}
}*/
//w zadaniu 6 dwa akumulatory